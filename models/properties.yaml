sources:
  - name: "proj"
    database: "DB_FINCRIME"
    schema: "SC_GOOGLEPOC"
    tables:
      - name: 'PARTY'

models:
  
  - name: 'project3.sql'
    config:
      materialized: 'table'
